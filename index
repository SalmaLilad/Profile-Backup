<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Saanvi · Applied AI & Computing Portfolio</title>
  <style>
    /* ====== THEME VARIABLES ====== */
    :root {
      --bg: #fafafa;
      --text: #12131a;
      --accent: #1f4aff;
      --panel: #f2f2f2;
      --muted: #555;
    }

    body.dark-mode {
      --bg: #12131a;
      --text: #e6e8f2;
      --accent: #ff4d4d;
      --panel: #1f1f25;
      --muted: #aaa;
    }

    /* ====== BASE STYLES ====== */
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      background-color: var(--bg);
      color: var(--text);
      transition: all 0.3s ease;
    }

    header {
      background: linear-gradient(90deg, var(--accent), #7f2aff);
      color: white;
      text-align: center;
      padding: 1.7rem 1rem;
      position: relative;
    }

    nav {
      background-color: var(--panel);
      text-align: center;
      padding: 1rem;
    }

    nav a {
      color: var(--text);
      text-decoration: none;
      margin: 0 1rem;
      font-weight: 600;
      transition: color 0.3s;
    }

    nav a:hover {
      color: var(--accent);
    }

    .toggle {
      position: absolute;
      right: 1rem;
      top: 1.2rem;
      background: none;
      border: 2px solid white;
      color: white;
      padding: 0.3rem 0.6rem;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      transition: background 0.3s, color 0.3s;
    }

    .toggle:hover {
      background: rgba(255,255,255,0.2);
    }

    section {
      max-width: 1000px;
      margin: 2rem auto;
      padding: 1rem;
      background-color: var(--panel);
      border-radius: 10px;
      transition: background 0.3s;
    }

    h2 {
      border-bottom: 2px solid var(--accent);
      padding-bottom: 0.3rem;
      color: var(--accent);
      transition: color 0.3s ease;
    }

    .project, .badge {
      background: white;
      border-radius: 8px;
      padding: 1rem;
      margin-bottom: 1rem;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      transition: transform 0.2s, background 0.3s;
    }

    body.dark-mode .project, body.dark-mode .badge {
      background: var(--panel);
      box-shadow: 0 2px 8px rgba(255,77,77,0.15);
    }

    .project:hover, .badge:hover {
      transform: translateY(-3px);
    }

    footer {
      text-align: center;
      background: linear-gradient(90deg, var(--accent), #7f2aff);
      color: white;
      padding: 1rem;
    }

    /* ====== CERTIFICATES & AFFILIATIONS ====== */
    .section-heading {
      border-bottom: 2px solid var(--accent);
      padding-bottom: 0.4rem;
      transition: color 0.3s, border-color 0.3s;
    }

    body.dark-mode .section-heading {
      color: #ff4d4d;
      border-color: #ff4d4d;
    }

    .badge-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
      text-align: center;
      margin-top: 1rem;
    }

    .badge-card {
      border: 1px solid #ccc;
      border-radius: 10px;
      padding: 0.8rem;
      transition: background 0.3s, transform 0.2s, border-color 0.3s;
      background: #fff;
    }

    body.dark-mode .badge-card {
      background: #2b2b2b;
      border-color: #444;
    }

    .badge-card:hover {
      transform: translateY(-3px);
    }

    /* ====== EXTRA SPACING FOR BULLETS ====== */
    #honors li, #achievements li {
      margin-bottom: 0.6rem; /* space between bullets */
      line-height: 1.5;
    }
  </style>
</head>

<body>
  <header>
    <h1>Saanvi</h1>
    <p>Aspiring Applied AI & Machine Learning Student's Portfolio</p>
    <button class="toggle" id="themeToggle">☀️ Light</button>
  </header>

  <nav>
    <a href="#about">About</a>
    <a href="#education">Education</a>
    <a href="#projects">Projects</a>
    <a href="#achievements">Enrichment</a>
    <a href="#honors">Awards</a>
    <a href="#skills">Skills</a>
    <a href="#github">GitHub</a>
    <a href="#contact">Contact</a>
  </nav>

  <section id="about">
    <h2>About Me</h2>
    <p>Hi! I'm Saanvi, a student exploring computer science, AI, and creative tech solutions for my community.</p>
  </section>

  <!-- Enrichment Programs -->
  <section id="achievements">
    <h2>Enrichment Programs</h2>
    <ul>
      <li>MIT BWSI Applied Engineering, AI & Future Technologies Program</li>
      <li>University of Minnesota Advanced AI/ML & Python Camps</li>
      <li>Kode With Klossy Data Science Camp</li>
      <li>CyberArts.io Cybersecurity CTF Winner</li>
      <li>Wolfram Alpha Summer Middle School Program</li>
    </ul>
  </section>

  <!-- Honors & Awards -->
  <section id="honors">
    <h2>Honors & Awards</h2>
    <ul>
      <li>2× National Spanish Exam Gold Medalist</li>
      <li>US Figure Skating Pre-Gold Level Passed; Training for Gold Skills Test</li>
      <li>Represent High School in the Minnesota Junior Winds Honor Band</li>
      <li>Percussionist with MacPhail Rimshots Ensemble</li>
      <li>Gold Award for Honors Reading (Grades 6–8)</li>
    </ul>
  </section>

  <!-- Certificates -->
  <section id="certificates">
    <h2 class="section-heading">Certificates & Badges</h2>
    <div class="badge-grid">
      <div class="badge-card"><img src="img/edx.png" alt="edX" width="70"><p>edX — Harvard CS50p</p></div>
      <div class="badge-card"><img src="img/umich.png" alt="U-Mich" width="70"><p>University of Michigan — Joy of Coding</p></div>
      <div class="badge-card"><img src="img/alphastar.png" alt="AlphaStar" width="70"><p>AlphaStar Intermediate Java</p></div>
      <div class="badge-card"><img src="img/collegeboard.png" alt="CollegeBoard" width="70"><p>College Board — AP Scholar</p></div>
    </div>
  </section>

  <!-- Affiliations -->
  <section id="affiliations" style="margin-top:2rem;">
    <h2 class="section-heading">Affiliations</h2>
    <div class="badge-grid">
      <div class="badge-card"><img src="img/aops.png" alt="AoPS" width="70"><p>AoPS</p></div>
      <div class="badge-card"><img src="img/bwsi.png" alt="MIT BWSI" width="70"><p>MIT BWSI</p></div>
      <div class="badge-card"><img src="img/fermilab.png" alt="Fermilab" width="70"><p>Fermilab</p></div>
      <div class="badge-card"><img src="img/umn.png" alt="UMN" width="70"><p>University of Minnesota</p></div>
    </div>
  </section>

  <footer>
    <p>© 2025 Saanvi | Coding with Curiosity & Creativity</p>
  </footer>

  <script>
    // ========== THEME HANDLER ==========
    const body = document.body;
    const btn = document.getElementById('themeToggle');

    // Check saved theme or start in dark by default
    const savedTheme = localStorage.getItem('theme');
    if (savedTheme === 'light') {
      body.classList.remove('dark-mode');
      btn.textContent = '🌙 Dark';
    } else {
      body.classList.add('dark-mode');
      btn.textContent = '☀️ Light';
    }

    // Toggle theme on click
    btn.addEventListener('click', () => {
      body.classList.toggle('dark-mode');
      const isDark = body.classList.contains('dark-mode');
      btn.textContent = isDark ? '☀️ Light' : '🌙 Dark';
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });
  </script>
</body>
</html>
